# Environment Configuration
ENVIRONMENT=development

# Database Configuration
DATABASE_URL=sqlite+aiosqlite:///./data/automa.db

# Security Configuration
# IMPORTANT: Generate a secure secret key for production!
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production-must-be-32-chars-minimum

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS Configuration
# For development (allows localhost)
CORS_ORIGINS=http://localhost:8002,http://localhost:8080
# For production (replace with your actual domains)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Sandbox Configuration
SANDBOX_IMAGE=automa-sandbox:latest
SANDBOX_MEMORY_LIMIT=256m
SANDBOX_CPU_LIMIT=0.5
SANDBOX_TIMEOUT=300

# File Paths
SCRIPTS_DIRECTORY=./scripts
DATA_DIRECTORY=./data

# Production Example:
# ENVIRONMENT=production
# SECRET_KEY=your-super-secure-random-32-char-secret-key-here
# DATABASE_URL=postgresql+asyncpg://user:password@db:5432/automa
# CORS_ORIGINS=https://automa.yourdomain.com